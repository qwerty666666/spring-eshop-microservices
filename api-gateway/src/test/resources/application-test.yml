spring:
  # Config Server
  cloud:
    config:
      enabled: false
    bus:
      enabled: false

  # Sleuth
  sleuth:
    sampler:
      probability: 0
    jdbc:
      p6spy:
        enabled: false

decorator:
  datasource:
    sleuth:
      enabled: false

# Eureka

eureka:
  client:
    enabled: false

# App

app:
  oas:
    url: /api
    spec-file-url: /openapi.yaml
