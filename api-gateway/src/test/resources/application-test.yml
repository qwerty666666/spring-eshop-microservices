# Config Server

spring:
  cloud:
    config:
      enabled: false
    bus:
      enabled: false

# Eureka

eureka:
  client:
    enabled: false

# App

app:
  oas:
    url: /api
    spec-file-url: /openapi.yaml
